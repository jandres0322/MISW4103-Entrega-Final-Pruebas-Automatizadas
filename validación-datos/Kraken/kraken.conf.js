module.exports = {
    concurrency: 1,  // Configura para que solo se ejecute un escenario a la vez
    deviceDirectory: '.kraken/.device_directory',  // Configura el directorio para dispositivos
    // Puedes agregar otras configuraciones según sea necesario, como configuraciones de navegador o rutas de dispositivos
  };
  